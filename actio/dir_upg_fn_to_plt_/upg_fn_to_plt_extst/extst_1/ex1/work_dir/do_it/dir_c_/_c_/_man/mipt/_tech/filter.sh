#!/bin/bash

#. "$HOME/.bashrc"

filename="$HOME/start/communis/Store/mipt/_tech/filter.sh"

echo -e "${HLIGHT}---start file: $filename ---${NORMAL}" # start file

#e_exer "Questions? See far!" "$filename" "$LINENO"

#up_lib_git ${FUNCNAME[0]}

cd $HOME/start/communis/Store/mipt/_tech/filter.sh || qq_exit

# cat sub_domens.txt | grep '!D'

for item in cat sub_domens.txt; do
if 
done

unset filename