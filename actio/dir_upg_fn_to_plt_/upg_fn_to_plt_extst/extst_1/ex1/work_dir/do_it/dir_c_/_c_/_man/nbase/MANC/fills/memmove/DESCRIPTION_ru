Функция memmove() скопирует n байтов из объекта, на который указывает s2, в объект, на который указывает s1. Копирование происходит так, как если бы n байтов из объекта, на который указывает s2, сначала копировались во временный массив из n байтов, который не перекрывает объекты, на которые указывают s1 и s2, а затем n байтов из временного массива копировались в объект, на который указывает s1.