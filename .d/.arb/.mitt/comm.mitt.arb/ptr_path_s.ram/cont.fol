
#! ptr_path
#local ptr_path="$1"

#if ! _abs_path "${PPWD}" ptr_path; then
#    echo "in fs= file://${HOME} , line=${LINENO}, ${FNN}() : : EXEC_FAIL : '_abs_path ${PPWD} ptr_path' : ${hint} : return 1"
#    return 1
#fi

#ptr_path="$(_abs_path "${PPWD}" "ptr_path")"