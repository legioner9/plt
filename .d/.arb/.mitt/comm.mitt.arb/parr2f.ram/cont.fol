#! ------ START parr2f ------

: >${file}
local item
local nn=1
IFS=$'\\n'
for item in ${arr[@]}; do
    echo "${nn} : ${item}" >>"${file}"
    nn=$((nn + 1))
done
IFS=$' \\t\\n'

#! ------- END parr2f -------

