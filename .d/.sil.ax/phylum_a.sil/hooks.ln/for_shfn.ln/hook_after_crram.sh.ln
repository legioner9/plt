#!/bin/bash
# use ${arb_} ${sil_} ${ram_name_to_create}

#! global: ram_name_to_create

cd ${arb_}/${ram_name_to_create}.ram/.grot || {
    plt_exit "EXEC_FAIL: cd ${arb_}/${ram_name_to_create}.ram/.grot return 3"
    return 3
}

${_cr_shfn_} ${ram_name_to_create}.sh
