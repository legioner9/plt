ARGS:
    \$1 (cntl (-n -an -t -at -t -m -c) [, \$2 mach_to_grep]) or \$1 ram_name

CNTLS:
        --_arb_path path to arb for grep
        -_

LOGIC ARGS:
    !()
    ?()

* !( a && b || c ) strong 
* ?() optional
* .() once
* {n,m}() regexp notam
exa:
    !( --_sil && --_arb )
    ( --_{sub}_{obs} && --_ram )
    ( --_{sub}_{obs} || -_crtag || -_addtag )
    ?( --_ram ) && (-_crtag || -_addtag )