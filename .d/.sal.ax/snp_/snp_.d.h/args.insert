ARGS:
    \$1 

CNTLS:
    strong:
        --_dir dir with {inserter}.insert
            if --_file not exist: echo info about .insert files
            if --_file exist: insert to that file all .insert files optional


    once(1InAll):

    optional:    
        --_file file with {inserter}