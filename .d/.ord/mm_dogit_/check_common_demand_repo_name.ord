#!/bin/bash

#? USE ${name_repo}

# if ! [ -d ${REPO_PATH}/${name_repo}/.ddd ]; then
#     plt_info "in check_common_demand_repo_name.ord : FAIL_EXEC : [ -d ${REPO_PATH}/${name_repo}/.ddd ] : return 1"
#     return 1
# fi

for file in $(d2e ${PLT_PATH}/.d/.ord/mm_dogit_/check_common_demand_repo_name.fn.lst.d); do

    echo -e "${GREEN}\$file = $file${NORMAL}" #print variable
     
done
