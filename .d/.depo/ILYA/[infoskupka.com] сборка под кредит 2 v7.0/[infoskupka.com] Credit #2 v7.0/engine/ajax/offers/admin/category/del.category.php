<?php
/*
=====================================================
 Файл: del.offers_post.php
=====================================================
 Данный код защищен авторскими правами
=====================================================
 Назначение: удаление объявления
=====================================================
*/

//-------------------------------------------------====
//	Инициализация
//-------------------------------------------------====

define( "ROOT_DIR", "../../../../.." );
define( "ENGINE_DIR", "../../../.." );
require_once( ENGINE_DIR."/ajax/offers/admin/OptAjax.php" );

//-------------------------------------------------====
//	Само удаление
//-------------------------------------------------====

$id = intval( $_REQUEST['id'] );
if( $id )
	{
		if( $Offers->DB['category'][ $id ] )
			{
				$db->query( "DELETE FROM ".PREFIX."_offers_options WHERE `id`='{$id}' LIMIT 1" );
				$Offers->CleareAllCache();
			}
				else
			{
				echoReturn( "Категория не существует, или была удалёна!" );
			}
	}
		else
	{
		echoReturn( "Не указан идентификатор категории!" );
	}



//-------------------------------------------------====
//	Завершение работы
//-------------------------------------------------====

$db->close();

?>